#include <iostream>
#include <stdio.h>       // printf,scanf
#include <stdlib.h>
#include <math.h>
#include <string>
#include <vector>
#include <algorithm>  //sort,binarySearch
#include <functional> 
#include <iomanip>    // setprecision
#include <utility>    // c+11 Array
#include <set>
#include <sstream>
#include <map>
#include <bits/stdc++.h>
#define MOD (1e+9)+7
#define PI 3.141592653589793238462643383279
#define FOR(i,a,b) for(int i=(a);i<(b);++i)
#define REP(i,n)  FOR(i,0,n)
#define pb(x) push_back(x)
#define debug(x) cout << #x << " = " << x << endl;
#define debret(x) debug(x) ; return 0;

using namespace std;

int main(void){

    string d  = "WBWBWWBWBWBW"; 
    string r  = "WBWWBWBWBWWB";
    string m  = "WWBWBWBWWBWB";
    string f  = "WBWBWBWWBWBW";
    string so = "WBWBWWBWBWWB";
    string l  = "WBWWBWBWWBWB";
    string si = "WWBWBWWBWBWB";
 
    string str;
    cin >> str;
    
    string key = str.substr(0,12);
    
    if(key == d)
        cout << "Do" << endl;
    if(key == r)
        cout << "Re" << endl;
    if(key == m)
        cout << "Mi" << endl;
    if(key == f)
        cout << "Fa" << endl;
    if(key == so)
        cout << "So" << endl;
    if(key == l)
        cout << "La" << endl;
    if(key == si)
        cout << "Si" << endl;
    
    return 0;
}
