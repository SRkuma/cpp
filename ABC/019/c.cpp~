#include <iostream>
#include <set>
#define FOR(i,a,b) for(long long int i =(a); i <(b);i++)
#define REP(i,N)   FOR(i,0,N)
#define debug(x)   cout << #x << " = " << x << endl;

using namespace std;

int main(void){

    set <int> st;
    int N;
    cin >> N;
    REP(i,N){
        int tmp;
        cin >> tmp;
        st.insert(tmp);
        st.insert(2*tmp);
    }
    return 0;
}
