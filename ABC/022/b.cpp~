#include <iostream>
#include <valarray>

#define FOR(i,a,b) for(long long int i =(a); i <(b);i++)
#define REP(i,N)   FOR(i,0,N)

using namespace std;

int main(void){

    int N;
    cin >> N;

    valarray <int> flower(N);

    REP(i,N){
        cout << flower[i] << endl;
    }

    return 0;
}
