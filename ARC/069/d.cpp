#include <iostream>
#include <vector>

#define FOR(i,a,b) for(long long int i = (a); i <(b);++i)
#define REP(i,n) FOR(i,0,n)
#define debug(x) cout << #x << " = " << x << endl;

using namespace std;

int main(void){

    int N;
    cin >> N;
    
    int  animal[N];
    char sameSide[N];
    char ans[N];
    
    return 0;
}
